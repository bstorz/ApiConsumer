# ApiConsumer

A simple wrapper around ZTTP (which in itself is a wrapper around Guzzle) for a very basic API that only communicates over GET, using Basic tokens.  Can be modified for whatever purpose necessary.
